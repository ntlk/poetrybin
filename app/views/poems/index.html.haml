- @poems.each do |poem|
  .poem-list__item
    %time.poem-list__date= poem.created_at
    .poem-list__link= link_to poem.title, poem_path(poem)
    - if logged_in?
      = link_to 'Hide', hide_poem_path(poem), method: 'PUT'
